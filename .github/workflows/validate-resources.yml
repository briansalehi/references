name: validate-resources
run-name: resource structure validation test
on: [push]
jobs:
  validation:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: logging
        run: echo "validating resources..."

      - name: validation
        run: bash validate.sh bookmarks/resources --quiet
